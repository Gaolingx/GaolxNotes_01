# 预训练后SFT效果差如何补救

### 🧑‍💻 User

llm跑了一个月预训练做完sft发现效果很差怎么办，还有补救措施吗

---

### 🤖 Assistant

跑了一个月预训练（Pre-training/PT），接着做完指令微调（SFT）发现效果很差，这确实非常让人崩溃，但这在模型训练中很常见。

**先吃一颗定心丸：SFT 效果差，不代表你那一个月的预训练白跑了。** 很多时候，问题出在 SFT 阶段的超参数、数据格式或者推理测试的设置上。

我们需要像医生排查疾病一样，从最容易解决的“表层问题”向“深层问题”逐一排查。以下是具体的诊断步骤和补救措施：

### 第一步：排查是否是“假象”（推理与评估设置问题）

有时候模型其实学好了，但你的测试方法不对，导致它表现得像个智障。

*   **Prompt Template（提示词模板）是否一致？** SFT 时你用的模板（如 ChatML、Alpaca 等，包含 `<|im_start|>` 等特殊 token），在推理测试时必须**一模一样**。少一个空格、换行，都会导致模型胡言乱语。
*   **生成超参数（Decoding Strategy）是否合理？** 检查推理时的 `temperature`、`top_p`、`repetition_penalty`。如果 `temperature` 太高（如 $>0.9$），模型会产生幻觉；如果 `repetition_penalty` 没设置好，模型可能会无限复读。建议先用 Greedy Search（`temperature=0`, `do_sample=False`）测试模型的最真实输出。

### 第二步：验证基座模型（Base Model）的能力（极其关键！）

这是决定你那一个月算力是否打水漂的核心步骤。不要拿 SFT 后的模型去测，**拿你 SFT 之前的那个预训练 Checkpoint 去测试**。

1.  **测续写能力：** 给基座模型输入一段维基百科、新闻或代码的前半段，看它能不能顺畅地续写出后半段。如果它能输出符合语法、逻辑连贯的文本，说明**预训练是成功的**，问题 100% 出在 SFT 阶段。
2.  **看 PPL（困惑度）：** 计算基座模型在通用验证集（如 WikiText, C4）上的 Perplexity。如果 PPL 降到了一个合理的范围（比如与同等参数量的开源模型相近），说明基座没问题。

<details>
<summary><b>展开查看：如果基座模型能力很差怎么办？（预训练失败的补救）</b></summary>

如果基座模型连话都说不通畅，那说明一个月的预训练确实出了问题。
*   **检查 Loss 曲线：** 预训练后期的 loss 是否还在下降？中间有没有出现尖峰（Spike）后 loss 一直下不去？如果是，可能是学习率过大、梯度爆炸或数据中有毒性/乱码数据。
*   **检查 Tokenizer：** 这是新手常犯的错误。检查 Tokenizer 对你的语料分词是否合理？特殊字符有没有被正确处理？
*   **补救措施（退回重训或继续预训练）：** 如果是后期 loss 崩了，可以退回到 loss 正常时的 Checkpoint，降低学习率（比如降为原来的 $1/10$）或者跳过那部分数据继续训练（Continual Pre-training），没必要从头再来。
</details>

### 第三步：诊断并重做 SFT（最有可能的补救点）

如果基座模型没问题，那恭喜你，一个月的心血保住了！SFT 训练成本通常很低（几个小时到几天），完全可以通过调整重新做。SFT 效果差通常是以下原因：

**1. SFT 数据质量问题（Garbage in, garbage out）**
*   **数据量与质量：** SFT 不需要海量数据，**质量远大于数量**。一万条高质量人工清洗的对话，远胜过一百万条粗糙的机器生成数据。检查数据中是否存在截断、格式错误、纯粹的“好的”、“我知道了”等低信息量回复。
*   **分布差异：** SFT 数据领域是否与预训练数据领域差异过大？

**2. SFT 超参数设置不当**
*   **学习率（Learning Rate）过大：** SFT 的学习率应该远小于预训练阶段。通常预训练可能是 $1e-4$ 级别，而全量 SFT 通常在 $1e-5$ 到 $5e-6$ 之间。如果 LR 太大，会引发**灾难性遗忘**（Catastrophic Forgetting），把预训练学到的知识洗掉。
*   **Epoch 数量过多：** SFT 极易过拟合。一般 $1 \sim 3$ 个 Epoch 就足够了。如果跑了 $5$ 个以上的 Epoch，模型可能只会死记硬背 SFT 数据的格式，泛化能力崩塌。
*   **Batch Size：** SFT 的 Batch Size 不宜过小，可以通过 Gradient Accumulation 增大等效 Batch Size。

### 总结：你的下一步 Action 计划

为了最高效地挽救项目，建议按照以下顺序操作：

1.  **停掉当前的训练，拉取 SFT 前的 Base 模型。**
2.  输入几段长文本，用 Base 模型做纯粹的**文本续写（Next-token prediction）**测试。
3.  **如果 Base 模型续写正常：**
    *   恭喜。清理你的 SFT 数据，抽样 1000 条人工检查。
    *   使用极低的学习率（例如 $1e-5$）和 1 个 Epoch 重新跑一次 SFT。
    *   如果算力紧张，**建议先使用 LoRA 或 QLoRA** 进行微调测试，这能让你在几个小时内快速验证 SFT 数据和参数是否合理。验证没问题后，再决定是否跑全量 SFT。
4.  **如果 Base 模型续写就是乱码/复读机：**
    *   查看预训练最后一周的 Loss 曲线，找到 Loss 发生异常的前一个 Checkpoint。
    *   检查这段时间喂给模型的数据，剔除脏数据。
    *   调低学习率（例如 $lr = lr * 0.5$），从那个健康的 Checkpoint **恢复预训练（Resume Training）**，再跑几天看看情况，切勿直接扔掉。