# 模型复述问题解决方法

### 🧑‍💻 User

sft阶段之后模型会先复述一遍用户问题怎么办？
例如：
prompt: 法国的首都在哪里？
completion: 法国的首都是什么？法国的首都是什么？法国的首都是巴黎。

---

### 🤖 Assistant

在 SFT（监督微调）阶段后，模型出现“先复述一遍用户问题”的现象，通常是因为训练数据的**模式泄漏**或**任务格式混淆**导致的。你的例子（输出“法国的首都是什么？法国的首都是什么？法国的首都是巴黎。”）不仅复述，还出现了重复，说明模型陷入了某种**模板循环**或**先确认再回答**的习惯。

以下是系统性分析及解决方案。

---

## 一、原因诊断

### 1. SFT 数据中嵌入了复述模板
很多对话数据集（如搜索问答、客服对话、指令跟随样本）会采用类似格式：
```
User: 法国的首都在哪里？
Assistant: 你问的是法国的首都对吧？法国的首都是巴黎。
```
或者模型被训练成“先确认用户意图再回答”。如果这类样本占比过高，SFT 就会学会这个模式。

### 2. 预训练阶段遗留的“复述”行为
大规模预训练语料（网页、论坛）中，常见“引用问题再回答”的格式（如百度知道、知乎），模型在预训练时已内化这种风格。若 SFT 阶段未彻底覆盖这种风格，它就会在微调后依然保留。

### 3. Prompt 结构不清晰
- 没有明确的用户/助手角色分隔符；
- 使用与训练时不一致的 prompt 模板；
- 系统指令缺失或冲突，导致模型无法区分“任务指令”和“需要直接回答的问题”。

### 4. 训练超参与数据比例问题
- **Epoch 过多**或**学习率偏高**，模型过拟合到某些高频复述样本上。
- 数据中直接回答的样本占比不足，模型统计上更倾向于“复述 → 回答”的路径。

---

## 二、解决方案（按优先级排序）

### ✅ 方案一：清洗/重组 SFT 数据（最根本）
**目标**：让“直接回答”成为绝大多数样本的标准模式。

具体操作：
- **自动检测并删除/重写**含复述的回复：
  - 如果 `assistant` 回复开头与 `user` 问题编辑距离小于某个阈值，则删除该样本或截断复述部分。
  - 示例脚本逻辑：
    ```python
    if reply.startswith(question) or reply.startswith("你问的是"):
        reply = reply.replace(question, "").strip("，。？")
    ```
- **增加高质量的直接回答样本**：
  - 用强模型（如 GPT-4）生成多种直接回答问题的风格作为辅助数据。
  - 确保这些样本不包含任何复述、反问、确认前缀。
- **数据比例控制**：确保直接回答样本占总 SFT 数据量至少 80% 以上，严重时可对复述类样本做下采样。

### ✅ 方案二：训练时加入针对性负样本或损失惩罚
- **构造拒复印本（Negative examples）**：
  - 对话格式：`User: ... \n Assistant: [复述问题]...`，将此类样本标注为“不期望的回复”，通过 DPO（Direct Preference Optimization）或 PPO 给予负反馈。
- **对回复前 N 个 token 施加重复惩罚**：
  在损失计算时，对前几个 token 与问题重复的程度增加惩罚权重（需自定义训练循环）。

### ✅ 方案三：推理侧干预（快速止损）
如果重新训练成本太高，可先用工程手段规避：

- **调整解码参数**：
  - 设置 `repetition_penalty`（如 1.1~1.2）抑制重复生成。
  - 降低 `temperature` 减少随机性，避免模型“游走”回复述模式。
- **Prompt 强化明确指令**：
  ```
  <|system|>你是一个直接回答问题的助手，不要复述用户的问题。</|system|>
  <|user|>法国的首都在哪里？</|user|>
  <|assistant|>
  ```
- **后处理过滤**：
  如果输出开头与输入问题高度相似，自动去掉开头重复部分再返回（临时方案）。

### ✅ 方案四：检查并统一 Prompt 格式
- 确保训练和推理时的 prompt 模板完全一致（如 `<|im_start|>user\n...<|im_end|>assistant\n`）。
- 如果使用 ChatML 或 Alpaca 格式，检查训练数据中是否有多余的空格、换行或特殊 token，它们可能被模型当作“开始复述”的信号。

### ✅ 方案五：对齐阶段修正（RLHF/DPO）
在 RLHF 阶段，将“复述问题”定义为一种**低奖励行为**：
- 训练一个奖励模型，对包含复述的回复打低分；
- 用 PPO 微调策略，显式抑制以问题片段开头的生成概率。

---

## 三、快速实验排查路径

1. **抽查 20 条训练样本**，看有多少条 assistant 回复是以复述开头的。
   - 若超过 10%，数据清洗是首要任务。
2. **用相同 prompt 在基座模型（未 SFT）上推理**，观察是否已有复述倾向：
   - 是 → 预训练风格残留，需加大 SFT 直接回答比例。
   - 否 → 纯粹由 SFT 数据引入，清洗即可解决。
3. **修改 prompt 为英文测试**（如 “What is the capital of France?”），若中文复述消失、英文正常，说明是中文数据特定问题，重点清洗中文 SFT 集。

---

通过以上组合手段，可以彻底消除模型“复述问题”的坏习惯。最核心的还是**严控 SFT 数据质量**，确保 Assistant 始终直奔主题。