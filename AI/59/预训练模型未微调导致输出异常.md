# 预训练模型未微调导致输出异常

### 🧑‍💻 User

llm预训练完的模型（未sft）明明loss很低（1.5左右），为什么模型输出完全是重复和乱码而不能回答问题？这是训练失败了吗，解码参数是temp 0.6，topk 20，topp0.95

---

### 🤖 Assistant

首先给你吃一颗定心丸：**这极大概率不是训练失败，而是未经过SFT（指令微调）的预训练基座模型（Base Model）的正常表现！**

预训练 Loss 降到 $1.5$ 左右通常说明模型已经学到了很好的语言规律（对应的困惑度 Perplexity $PPL = e^{1.5} \approx 4.48$，这是一个相当不错的数值）。它之所以表现得像“人工智障”，完全是因为**预训练模型和聊天模型的工作原理不同**。

以下是导致这种现象的根本原因，以及如何正确测试你的模型。

### 1. 为什么基座模型不能“回答问题”？

预训练（Pre-training）的唯一目标是 **Next Token Prediction（预测下一个词）**。它吸收了海量的互联网网页、书籍、代码，学习的是“如何把一段话顺畅地接续下去”，而不是“如何回答用户的提问”。

*   **你以为的逻辑：**
    *   输入：`"中国的首都是哪里？"`
    *   期望输出：`"中国的首都是北京。"`
*   **基座模型的逻辑：**
    *   输入：`"中国的首都是哪里？"`
    *   模型在训练数据中见过类似格式的网页（比如考试题库），它认为接下来应该继续出题。
    *   实际输出可能变成：`"美国的首都是哪里？日本的首都是哪里？英国的首都是哪里？..."`（导致**重复**）。

因为没有经过 SFT，模型不知道自己是一个“AI 助手”，也不知道在回答完问题后应该输出 `<EOS>`（结束符）来停止生成，所以它会一直顺着上下文瞎编（导致**乱码或无意义的长篇大论**）。

### 2. 如何正确测试预训练模型（Base Model）？

要测试你的模型是否成功，**不要用问答的形式，要用“文本续写”的形式**。

你可以尝试用以下几种 Prompt 来测试（你的解码参数 `temperature=0.6`, `top_k=20`, `top_p=0.95` 是合理的，但建议加上**重复惩罚**）：

*   **测试知识抽取（续写事实）：**
    *   Prompt: `"中国的首都是北京。法国的首都是巴黎。英国的首都是"`
    *   正常预期输出：`"伦敦。..."`
*   **测试语言建模（续写文章）：**
    *   Prompt: `"在人工智能飞速发展的今天，大语言模型已经成为了"`
    *   正常预期输出：`"科技领域的热门话题。许多科技公司纷纷投入..."`
*   **测试代码能力（如果训练语料有代码）：**
    *   Prompt: `"def fibonacci(n):"`
    *   正常预期输出：接下来的斐波那契数列代码实现。

**💡 关键解码参数调整：**
基座模型极易陷入复读机循环。在你的解码参数中，强烈建议加入 **Repetition Penalty（重复惩罚）**。
```python
generation_config = {
    "temperature": 0.6,
    "top_k": 20,
    "top_p": 0.95,
    "repetition_penalty": 1.15, # 加上这个参数，通常设置在 1.1 ~ 1.2 之间
    "max_new_tokens": 100
}
```

---

<details>
<summary><b>🚨 进阶排查：如果“文本续写”依然是纯乱码，可能是哪些问题？（点击展开）</b></summary>

如果在采用了“文本续写”和“重复惩罚”后，模型输出的依然是毫无人类语言逻辑的纯乱码（比如 `“的 的 猹 猹 > > & &”`），那么确实可能是训练或推理环节出了 Bug。请排查以下几点：

1.  **Tokenizer（分词器）不匹配：**
    这是最常见的问题！推理时加载的 Tokenizer 必须和训练时使用的 Tokenizer 完全一致。如果词表映射错了，模型输出的 token id 解码成文字就会变成乱码。
2.  **推理代码的 Padding/BOS/EOS 设置错误：**
    检查输入是否正确添加了 `<s>` (BOS) token。如果模型训练时总是依赖起始符，而推理时没加，输出会崩坏。
3.  **数据质量问题（Loss 虚假降低）：**
    Loss $1.5$ 确实很低。但如果你的训练数据有很多 padding（比如大量 `<pad>` 标签被计算进了 loss），或者数据中重复文本极多，模型会很快过拟合这些无意义的 token，导致 Loss 看起来很低，但实际什么都没学到。
4.  **精度溢出或模型权重损坏：**
    检查模型保存时是否发生精度截断（例如 Bf16 存成了 Fp16 导致溢出），或者 Checkpoint 加载不完整。
</details>

---

### 下一步你应该做什么？

既然预训练的 Loss 已经降到了很低的水平，你的基座模型大概率已经是一个“满腹经纶但不懂规矩”的毕业生了。

**你的下一步必须是进行 SFT（Supervised Fine-Tuning，监督微调）。**

你需要构建问答格式的数据集（如 Alpaca 格式、ShareGPT 格式），用明确的系统提示词（System Prompt）、用户输入（User）和模型回答（Assistant），以及关键的 `<EOS>` 结束符来微调模型。

例如，SFT 数据长这样：
```json
{
  "instruction": "请问中国的首都是哪里？",
  "output": "中国的首都是北京。</s>"
}
```
经过 SFT 之后，模型就会明白：“哦！原来我需要回答问题，并且回答完之后要立刻闭嘴（输出 `</s>`）”。这时候，它才是一个真正可用的 LLM！

---

### 🧑‍💻 User

为什么sft阶段做完之后，模型对解码参数敏感性显著降低？

---

### 🤖 Assistant

这是一个非常敏锐且专业的观察！你在实践中发现了大模型训练中最核心的现象之一。

简单来说，SFT（监督微调）阶段做完之后，模型对解码参数（Temperature, Top-k, Top-p）敏感性显著降低的根本原因是：**SFT 极大地改变了模型输出词汇的“概率分布”，使其从“平缓发散”变成了“极端尖锐”。**

我们可以从以下几个维度来深入理解这个现象：

### 1. 核心数学原理：概率分布的“坍缩”（熵减）

在理解这个问题前，我们需要回顾解码参数是如何起作用的。解码参数本质上是在干预模型输出层的 **Logits（原始得分）** 转化为 **Probabilities（概率）** 的过程。

*   **基座模型（Base Model）的概率分布：平缓（高熵）**
    基座模型看过无数不同的网页。对于输入 `"天空是"`，它可以接 `"蓝色的"`，也可以接 `"灰蒙蒙的"`，甚至可以接 `"一首由某某演唱的歌曲"`。
    此时模型预测下几个 Token 的概率可能是：
    $P(蓝色的) = 0.3, \quad P(灰色的) = 0.25, \quad P(一首) = 0.2$
    在这种**平缓**的分布下，你稍微调整 `temperature` 放大差异，或者调整 `top-k` 截断候选词，模型最终采样出来的词就会发生剧烈变化。

*   **SFT 模型的概率分布：尖锐（低熵）**
    SFT 的数据格式是高度统一的问答对。当你输入 `"请问法国的首都是哪里？"` 时，SFT 强制模型学习到，此时唯一正确的回答就是 `"法国的首都是巴黎。"`。
    经过 SFT，模型预测下一个 Token 的概率会变成：
    $P(巴黎) = 0.99, \quad P(里昂) = 0.005, \quad P(伦敦) = 0.001$
    在这种**极端尖锐**的分布下，因为 $0.99$ 的优势太大了，无论你把 `temperature` 设为 $0.1$ 还是 $0.9$，经过 Softmax 计算后，`"巴黎"` 的概率依然占据绝对统治地位；无论 `top-k` 是 $10$ 还是 $50$，系统绝大概率还是会采样到 `"巴黎"`。

### 2. SFT 赋予了模型强大的“格式惯性”

SFT 阶段不仅教模型回答问题，更重要的是教模型**遵守格式**（例如 ChatML 格式、Alpaca 格式）。

在基座模型阶段，模型不知道什么时候该换行，什么时候该停止。
而在 SFT 模型中，如果它输出了 `<|im_start|>assistant`，接下来的分布会立刻收敛到回答问题的逻辑中；当它把答案说完后，预测 `<EOS>`（结束符）的概率会瞬间飙升到 $0.999$ 以上。

这种结构化的训练让模型生成文本的“路径”被大大限制了。解码参数失去了让模型“自由发挥”的空间，因为偏离正确格式和答案的 Token 的概率被 SFT 压得无限趋近于 $0$。

### 3. SFT 削弱了“多模态”倾向（这里的模态指文体风格）

预训练数据包含代码、小说、新闻、维基百科、论坛对骂。基座模型在解码时，很容易因为参数的微调而在不同的“文体”之间跳跃（比如突然从写新闻变成写代码）。

SFT 用高质量、统一风格的对话数据（通常是“有礼貌的 AI 助手”风格）对模型进行了“洗脑”。模型被锁定在了一种单一的文体风格中。既然只有一种风格可以选，解码参数自然也就“无用武之地”了。

---

<details>
<summary><b>💡 进阶探讨：完全不敏感了吗？（点击展开）</b></summary>

虽然 SFT 模型对参数不那么敏感了，但这**并不绝对**。敏感性取决于**具体的任务类型**以及 **SFT 的训练程度**。

1.  **任务差异导致敏感度不同：**
    *   **数学、逻辑、事实问答：** 极度不敏感。因为答案是唯一的（如 $1+1=2$），模型在这个空间的概率分布最尖锐。
    *   **创意写作、头脑风暴：** 依然具有一定的敏感性。如果你让模型 `"写一首关于秋天的现代诗"`，由于没有绝对正确的标准答案，模型的概率分布会相对平缓一点，此时调高 `temperature` 依然能让诗歌更有创意。

2.  **SFT 过拟合（Overfitting）/ 模式崩溃（Mode Collapse）：**
    如果你发现模型对解码参数**彻底免疫**，哪怕把 `temperature` 开到 $2.0$ 它都输出一模一样的话，这说明你的 SFT **过拟合了**。
    模型丧失了预训练阶段积累的多样性（Diversity），这在 RLHF（强化学习人类反馈）阶段尤为常见（被称为“对齐税” Alignment Tax）。这往往意味着 SFT 的 Epoch 过大，或者 Learning Rate 偏高。

3.  **Softmax 温度缩放公式回顾：**
    $$ P(x_i) = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)} $$
    其中 $z_i$ 是 Logits， $T$ 是 Temperature。
    SFT 的作用是让目标答案的 $z_i$ 变得极大。当 $(z_{\text{target}} - z_{\text{other}})$ 的差值极其巨大时，常规范围内的 $T \in [0.1, 1.0]$ 已经无法有效改变 Softmax 后的相对概率大小了。

</details>

### 总结

SFT 让大模型从一个“天马行空的吟游诗人（Base）”变成了一个“严谨死板的应试学生（SFT）”。

由于“应试学生”只认标准答案和标准格式，他脑海中的候选词（概率分布）变得非常少且确定。因此，无论你怎么调整解码参数试图鼓励他“发散思维”，他都会固执地给出他认为唯一正确的那个答案。这就是参数敏感性降低的根本原因。