# SFT初期loss梯度毛刺属正常现象

### 🧑‍💻 User

llm基座第一轮sft lmloss和gradnorm初期出现spike（毛刺）又马上回落属于正常现象吗？

---

### 🤖 Assistant

在LLM基座模型进行第一轮SFT（指令微调）时，初期`lm_loss`和`grad_norm`出现毛刺（Spike）然后又马上回落，**属于非常正常的现象，且在实际训练中极为常见。**

这种情况通常被称为“初始冲击（Initial Shock）”或“分布偏移适应”。以下是导致这一现象的核心原因：

### 为什么会出现初期Spike？

1. **数据分布的突然改变 (Distribution Shift)**
   基座模型在预训练阶段看到的是连续的、无固定格式的自然语言文本。而在SFT阶段，数据突然变成了结构化的格式（如Prompt-Response、User-Assistant对话模板）。这种输入分布的突变会让模型在最初几个Step感到“惊讶”，导致预测偏差增大，从而产生极高的`lm_loss`。由于Loss陡增，误差反向传播时自然会导致`grad_norm`（梯度范数）随之飙升。
2. **全新特殊Token的引入**
   SFT通常会引入新的控制字符（如 `<|im_start|>`, `<|im_end|>`, `[INST]` 等）。这些新Token的Embedding通常是随机初始化的，或者虽然继承了某些值但并未针对当前语境优化过。在最初的几次更新中，这些未充分训练的参数会产生极大的梯度。
3. **优化器状态 (Optimizer State) 尚未建立**
   如果是使用 `Adam` 或 `AdamW` 等一阶动量和二阶动量优化的算法，在Step 0时，优化器的内部状态（动量和方差估计）全为0。在最初的几个Batch中，由于缺乏历史梯度的平滑作用，参数更新方向可能会比较剧烈和抖动。
4. **损失截断（Loss Masking）的突然生效**
   在SFT中，我们通常只对Target（回答部分）计算Loss，对Prompt部分进行Mask（Loss权重设为0）。模型在预训练时习惯了预测所有Token，这种计算方式的改变也会在初期引起梯度的波动。

---

<details>
<summary><strong>💡 展开查看：如何判断Spike是否处于“健康”状态？</strong></summary>

虽然初期Spike是正常的，但你需要观察它的恢复情况来判断训练是否健康：

*   **健康的Spike：**
    *   **发生时间：** 仅出现在训练的极早期（通常在最初的 10 到 100 个 Steps 之间）。
    *   **恢复速度：** `lm_loss` 和 `grad_norm` 能够迅速且平滑地回落到一个合理的基线水平，并开始呈现稳定的下降趋势。
    *   **梯度裁剪触发：** `grad_norm` 可能会在几步内触碰你设置的 `max_grad_norm`（例如 $1.0$），但随后很快降至阈值以下。
*   **不健康的Spike（需要警惕）：**
    *   **持续不降：** Spike发生后，Loss在高位震荡，迟迟不下降。
    *   **梯度爆炸：** `grad_norm` 持续增大，最终导致 `lm_loss` 变成 `NaN` 或 `Inf`。
    *   **中后期复发：** 训练平稳了一段时间后，中途突然出现巨大的Spike且无法快速恢复（这通常意味着数据集中存在脏数据、超长截断异常或学习率设置过高）。
</details>

<details>
<summary><strong>🛠️ 展开查看：如何缓解或优化这种初期震荡？</strong></summary>

如果你觉得初期的Spike过大，担心影响模型权重的稳定性，可以采取以下工程手段：

1.  **使用学习率预热 (Learning Rate Warmup)：**
    这是最关键的手段。设置 `warmup_steps` 或 `warmup_ratio`（例如前 $3\%$ 到 $5\%$ 的步骤），让学习率从 $0$ 慢慢线性增加到最大学习率。这给优化器收集统计信息的时间，极大缓解初始分布冲击。
2.  **设置梯度裁剪 (Gradient Clipping)：**
    在训练配置中开启梯度裁剪（如 `max_grad_norm = 1.0`）。这可以硬性限制梯度的最大 $L_2$ 范数，防止初期的巨大梯度直接“破坏”预训练好的基座权重。
3.  **部分冻结 / 延迟解冻：**
    如果引入了大量新的特殊Token，可以尝试在最开始的几百步只训练 `Embedding` 层和 `lm_head` 层，然后再放开全部参数进行全量微调（不过在使用Warmup的情况下，这一步通常不是必须的）。
4.  **检查数据洗牌 (Data Shuffling)：**
    确保SFT数据在打乱后喂给模型。如果前几个Batch恰好全是极长或者极难的样本，也会人为导致严重的Spike。
</details>

**总结：** 只要这个毛刺发生在训练刚刚开始的前几十步内，并且能够迅速回落，开启正常的收敛曲线，你完全可以忽略它，放心地继续训练。