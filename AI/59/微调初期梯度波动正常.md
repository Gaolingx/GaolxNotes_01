# 微调初期梯度波动正常

### 🧑‍💻 User

llm base首轮sft前100步 gradnorm冲到10-20，loss 2-3然后快速下降属于正常现象吗？

---

### 🤖 Assistant

这种表现属于**正常现象**，在预训练基座模型（base model）上进行首次监督微调（SFT）时尤其常见。下面把原因、机制和注意事项拆开说清楚。

---

## 1. 为什么 loss 2–3 是合理起点？
- 交叉熵 loss 的理论上限是 **log(vocab_size)**，对于 vocab_size ≈ 30k–100k，上限约 10.3–11.5。
- 实际 SFT 初始 loss **通常落在 2–4 之间**，对应 perplexity 大约 $e^2≈7.4$ 到 $e^3≈20$。
- Base 模型已经具备较强语言能力，只是对**指令格式、特殊分隔符、对话角色标记**（如 `<|assistant|>`）可能不够熟练，因此 loss 从 2–3 起步非常常见，并不算高。

## 2. 前 100 步 gradnorm 冲到 10–20 是为什么？
这里假设你观察的是**所有参数梯度的全局 L2 范数**（未裁剪前的值）。10–20 这个量级通常**不算大**，而且早期冲高是模型**快速适应新分布**的自然表现：

- 最初几步，模型对 SFT 数据中突然出现的**新 token 或罕见 token**（`<|im_start|>`、`<|assistant|>` 等）几乎完全陌生，softmax 输出概率极低，导致这些 token 位置的回传梯度很大。
- 同时，对话/指令的**回答部分**通常需要模型纠正原有的自由生成习惯（比如从长篇无目的续写变成直接回答问题），也会产生较强的梯度信号。
- 优化器在最初几步完成“大方向修正”后，目标 token 的概率迅速上升，梯度范数随之快速下降。因此你会看到 **gradnorm 先冲高再回落，loss 同时快速下降**——这正是模型在有效学习新格式的信号。

如果使用了 warmup（学习率预热）且梯度裁剪阈值设置在常见值（如 1.0 或 10.0），裁剪前的真实 gradnorm 冲到 10–20 甚至更高都**完全可以接受**，只要：
- 没有出现 **NaN / Inf**
- loss 趋势持续向下
- 几次 step 后 gradnorm 回落到个位数或以下

## 3. 快速下降是健康的吗？
**是**。前几百步的 loss 快速下降，主要源于：
- 模型迅速学会高频、简单的格式规则（比如在哪开始生成回答）。
- 简单模式（标点、常见短回复）的迁移成本很低。
- 如果是小学习率 + Adam，这种“快速收益期”通常持续几十到几百步，然后转入平缓下降。

## 4. 什么情况需要警惕？
虽然你说的情况正常，但仍建议关注以下几点以防异常：

| 现象 | 可能原因 | 处理建议 |
|------|----------|----------|
| gradnorm 持续 > 100 且不回落 | 学习率过大、数据有脏样本、特殊 token 未加入词表 | 降低 LR、增加 warmup 步数、检查 tokenizer 添加的特殊 token 是否已初始化 embedding |
| loss 突然跳升到 10+ 且不回来 | 梯度爆炸、数据格式错乱 | 查看梯度裁剪是否生效、数据中是否混入非法字符 |
| gradnorm 被裁剪死死压住（比如一直等于 clip_value） | 真实梯度远大于裁剪阈值，可能错过优化方向 | 可适当放宽梯度裁剪阈值（如调到 10.0），并配合 Adam 自适应学习率 |
| loss 很快跌到极低（<1.0）并在几个 step 后过拟合 | 数据量太小、重复度过高 | 正常，微调阶段 loss < 1.0 也常见，关注 eval loss 止跌反升即可 |

---

### 结论
**前 100 步 gradnorm 冲到 10–20，loss 2–3 随后快速下降，是 SFT 初期的典型健康行为。** 这表示模型在新指令数据上快速对齐格式与风格，无需干预。继续观察 loss 和 gradnorm 的趋势，确保它们最终平稳下降即可。