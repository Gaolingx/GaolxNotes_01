# 无特殊标记影响工具调用稳定性

### 🧑‍💻 User

在对llm进行tool calling sft微调的时候，如果模型的tokenizer config没有专门为tool call注册对应的special token，是否会影响推理阶段tool calling？
例如dataset包含以下Json schema
```json
{
    "conversations": [
        {"role": "user", "content": "你好"},
        {"role": "assistant", "content": "你好！"},
        {"role": "user", "content": "再见"},
        {"role": "assistant", "content": "再见！"}
    ]
}
{
    "conversations": [
        {"role": "system", "content": "# Tools ...", "tools": "[...]"},
        {"role": "user", "content": "把'你好世界'翻译成english"},
        {"role": "assistant", "content": "", "tool_calls": "[{\"name\":\"translate_text\",\"arguments\":{\"text\":\"你好世界\",\"target_language\":\"english\"}}]"},
        {"role": "tool", "content": "{\"translated_text\":\"Hello World\"}"},
        {"role": "assistant", "content": "Hello World"}
    ]
}
```

---

### 🤖 Assistant

**直接回答：**
**会产生影响，但并非绝对不能工作。** 

如果 tokenizer 中没有注册专门的 tool call special tokens（例如 `<tool_call>`, `<tool_response>` 等），模型依然可以通过学习文本模式（Pattern）来掌握 Tool Calling 能力（即**基于纯文本/JSON的 Tool Calling**）。但是，这会在**推理阶段（Inference）的解析、停止条件设置以及鲁棒性**上带来一系列具体的挑战和影响。

以下是详细的影响分析以及应对策略：

<details open>
<summary><b>1. 对推理阶段产生的主要影响（缺点）</b></summary>

没有 Special Token 作为清晰的边界，推理阶段的工程实现会变得更加复杂：

*   **停止条件（Stop Criteria）难以判定：** 
    有了 special token，你可以直接将 `<tool_call_end>` 的 token ID 设为停止词，生成一旦遇到它就暂停，交由业务逻辑执行 API。如果没有，模型可能会在生成完 JSON 后继续生成普通回复（比如："我已经调用了工具..."），你需要依赖通用的 `<|im_end|>` 或者复杂的正则表达式来截断和提取工具调用。
*   **解析难度与鲁棒性降低：**
    在没有特殊标记的情况下，你需要从模型的 `assistant` 回复中用正则（Regex）提取 JSON 字符串。模型可能会在 JSON 前后“夹带私货”（例如：`Here is the tool call: {"name":...}`），导致 `json.loads()` 报错崩溃。
*   **上下文长度与 Token 开销：**
    Special token 通常是一个单一的 token（如 ID: 32001）。如果没有它，模型为了表示工具调用的开始和结束，可能需要生成一段类似于 `\n```json\n` 或 `<tool>` 的普通文本，这会消耗更多的 token 数量，降低推理效率。
*   **角色混淆（Role Confusion）：**
    在你的例子中，有一个 `{"role": "tool"}`。如果没有对应的 special token 来明确标识这是一个工具返回结果，模型只能依赖诸如 `[TOOL_RESULT]` 这样的普通字符串来理解上下文。在极少数情况下，如果用户恶意输入相同的字符串，可能会引发 Prompt Injection。

</details>

<details open>
<summary><b>2. 为什么没有 Special Token 也能 Work？（背后的原理）</b></summary>

在你的 Dataset 示例中，实际上隐含了一个 **Chat Template** 的转换过程。在 SFT 时，这些 JSON 数据会被拼接成一根长字符串喂给模型。

如果你的 Chat Template 是这样映射的：
```text
<|im_start|>system
# Tools ... [...]<|im_end|>
<|im_start|>user
把'你好世界'翻译成english<|im_end|>
<|im_start|>assistant
{"name":"translate_text","arguments":{"text":"你好世界","target_language":"english"}}<|im_end|>
<|im_start|>tool
{"translated_text":"Hello World"}<|im_end|>
<|im_start|>assistant
Hello World<|im_end|>
```
模型在 SFT 阶段**学习到的是一种纯粹的“文本补全（Next-token prediction）”模式**：当系统提示词中包含 Tools 定义时，它会倾向于直接输出符合要求的 JSON 字符串。这在业界被称为 **JSON-based Tool Calling**（例如早期的 Llama 2 或部分未专门适配的开源模型都是如此做的）。

</details>

<details>
<summary><b>3. 推理阶段的工程补救措施（如果没有 Special Token）</b></summary>

如果你决定不修改 tokenizer（因为修改 tokenizer 通常需要 resize 模型的 embedding 层，并可能带来轻微的灾难性遗忘风险），你在推理阶段需要做好以下工程适配：

1.  **强依赖 System Prompt：**
    在 System 提示词中必须严厉地规定格式，例如：*“当你需要调用工具时，必须且只能输出严格的 JSON 格式，不要包含任何其他解释性文字。”*
2.  **使用结构化生成（Structured Generation / Constrained Decoding）：**
    在推理引擎（如 vLLM, llama.cpp, SGLang）中开启 **JSON Schema 约束（Guided Decoding）**。通过传入期望的 JSON 格式，强迫模型在预测 token 时只生成合法的 JSON 字符，彻底避免解析失败。
3.  **定制化 Parser 逻辑：**
    你需要编写更健壮的代码来处理模型的输出。
    ```python
    import re
    import json

    def parse_tool_call(assistant_text):
        # 使用正则提取文本中可能存在的 JSON block
        match = re.search(r'\{.*"name":.*"arguments":.*\}', assistant_text, re.DOTALL)
        if match:
            try:
                return json.loads(match.group(0))
            except json.JSONDecodeError:
                return None
        return None
    ```
4.  **严格对齐 Chat Template：**
    训练时如何拼接这些 JSON 字段（如你的 `tool_calls` 字段在底层被转换成了什么具体字符串），推理时必须保持**绝对一致**。

</details>

### 总结建议

虽然没有专属的 special token 不会让你 SFT 彻底失败，但**业界目前的最佳实践（Best Practice）强烈建议为 Tool Calling 注册专属的 Special Token**（例如 Qwen, Llama 3 等都在 Tokenizer 层面做了专门的支持）。

它能大幅降低推理端（Inference）代码的编写难度，提高系统调用的成功率，并减少因模型输出不稳定导致的 JSON 解析报错。如果条件允许，建议在 SFT 之前对 Tokenizer 进行扩充（`add_special_tokens`）。

---

### 🧑‍💻 User

如果预训练阶段没有用到tool call spec token，在sft阶段之前的`add_tokens_decoder`补充就行了吗？

---

### 🤖 Assistant

**直接回答：**
**可以，这也是目前业界非常标准和常见的做法。** 很多开源基础模型（Base Model）在预训练时并没有 Tool Calling 的概念，这些特殊 Token 都是在 SFT 阶段硬加进去的。

但是，**仅仅在 Tokenizer 中补充（`add_special_tokens`）是绝对不够的**。为了让模型能正常训练和推理，你必须在代码层面完成以下关键步骤，否则会导致训练崩溃（如 CUDA Out of Memory 或 Index Out of Bounds）或者模型根本学不会这些 Token。

以下是具体的必要操作步骤和注意事项：

<details open>
<summary><b>1. 必须调整模型词表大小（Resize Token Embeddings）</b></summary>

当你在 Tokenizer 中添加了新的 Special Token 后，Tokenizer 的词表大小（Vocab Size）增加了。但是，模型底层的 Embedding 矩阵（形状为 $V \times d$，其中 $V$ 是词表大小，$d$ 是隐藏层维度）和输出层（`lm_head`）的维度并没有变。

因此，在加载模型后，**必须**调用扩容方法：

```python
from transformers import AutoTokenizer, AutoModelForCausalLM

tokenizer = AutoTokenizer.from_pretrained("your-model")
model = AutoModelForCausalLM.from_pretrained("your-model")

# 1. 在 tokenizer 中添加 special token
special_tokens_dict = {'additional_special_tokens': ['<tool_call>', '<tool_response>']}
num_added_toks = tokenizer.add_special_tokens(special_tokens_dict)

# 2. 【核心步骤】调整模型的 Embedding 层大小
model.resize_token_embeddings(len(tokenizer))
```
这个操作会在模型底层的 `embed_tokens` 和 `lm_head` 矩阵中追加对应数量的行/列。

</details>

<details open>
<summary><b>2. 如果使用 LoRA 微调，必须放开 Embedding 层的训练</b></summary>

如果你使用的是全量微调（Full Fine-Tuning），新增的 Embedding 参数自然会跟着更新。
但如果你使用的是 **LoRA / QLoRA** 微调，默认情况下 LoRA 只会微调 Attention 层或 MLP 层的权重。**新增加的 Token 对应的 Embedding 是随机初始化的**，如果不去训练它们，模型输出遇到这些 Token 时就会是一团乱码。

因此，在配置 LoRA 时，必须将 Embedding 层和 LM Head 层加入到 `modules_to_save` 中，强制这部分参数参与梯度更新：

```python
from peft import LoraConfig

lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj", "k_proj", "o_proj"], # 根据你的模型架构调整
    # 【核心步骤】必须把 embedding 和 lm_head 加进来
    modules_to_save=["embed_tokens", "lm_head"], 
    bias="none",
    task_type="CAUSAL_LM"
)
```

</details>

<details>
<summary><b>3. 高阶技巧：新 Token 的 Embedding 初始化（加速收敛）</b></summary>

当你执行 `resize_token_embeddings` 时，新增加的 Token 对应的向量值是**完全随机初始化**的。在 SFT 数据量较小的情况下，模型可能很难在短时间内把这个随机向量优化到一个合理的位置（表现为 Loss 很高，或者模型死活不输出 `<tool_call>`）。

**最佳实践技巧（NEFTune / 均值初始化）：**
在训练前，手动将新 Token 的 Embedding 初始化为某些现有相关 Token（例如 `<|im_start|>`、`{` 或者 `tool`）的均值。这能极大加速收敛。

```python
import torch

# 假设新增了两个 token，获取它们现在的随机 embedding
input_embeddings = model.get_input_embeddings().weight.data
output_embeddings = model.get_output_embeddings().weight.data

# 获取参考 token 的 ID（比如使用普通的文本 "tool" 和 "{" 来做参考）
reference_token_ids = tokenizer.convert_tokens_to_ids(["tool", "{"])
reference_embeddings = input_embeddings[reference_token_ids].mean(dim=0)

# 把新增 token 的 embedding 替换为参考 token 的均值
# 假设新增 token 的 ID 是当前 tokenizer 的最后两个
new_token_id_start = len(tokenizer) - num_added_toks

for i in range(new_token_id_start, len(tokenizer)):
    input_embeddings[i] = reference_embeddings.clone()
    output_embeddings[i] = reference_embeddings.clone()
```

</details>

### 总结
在 SFT 阶段补充 Special Token 完全可行且推荐。关键在于：**加了 Token -> 必须 Resize 模型维度 -> 如果用 LoRA 必须放开对应层的训练 -> 最好做一下均值初始化**。只要做好这四点，就能完美实现带有专属 Token 的 Tool Calling 微调。

---

### 🧑‍💻 User

tokenizer_config.json中的added_tokens_decoder和additional_special_tokens有什么区别？

---

### 🤖 Assistant

**直接回答：**

在 `tokenizer_config.json` 中，`additional_special_tokens` 和 `added_tokens_decoder` 都与新增的特殊 Token 有关，但它们的**抽象层级**和**作用阶段**完全不同：

*   **`additional_special_tokens`**：是**高层的、面向用户的意图声明**。它告诉 Tokenizer 哪些字符串是特殊的（编码时不要切碎它们，解码时可以通过参数忽略它们）。
*   **`added_tokens_decoder`**：是**底层的、面向解码器的具体规则映射**。它定义了具体的 Token ID 在被解码（转换回文本）时的精确行为（例如是否要去除前后的空格）。

以下是详细对比与解析：

<details open>
<summary><b>1. additional_special_tokens（高层声明 / 偏向 Encoding）</b></summary>

*   **数据结构：** 通常是一个简单的字符串列表（List of Strings）。
*   **主要作用：** 
    当你将一段文本输入给模型（Encoding 阶段）时，Tokenizer 的核心正则/BPE算法如果看到这些字符串，就会把它们当做一个整体，**绝对不会切分（Split）它们**。
    同时，它标记了这些 Token 的属性为 `special=True`，这样在调用 `tokenizer.decode(..., skip_special_tokens=True)` 时，它们会被自动过滤掉。
*   **示例：**
    ```json
    "additional_special_tokens": [
      "<tool_call>",
      "<tool_response>",
      "<|im_start|>"
    ]
    ```

</details>

<details open>
<summary><b>2. added_tokens_decoder（底层映射 / 偏向 Decoding）</b></summary>

*   **数据结构：** 一个字典（Dict），键（Key）是字符串格式的 **Token ID**，值（Value）是一个复杂的对象（对应底层库中的 `AddedToken` 类）。
*   **主要作用：**
    当模型输出一串 ID，Tokenizer 需要将其转换回人类可读的文本（Decoding 阶段）时，它会查阅这个字典。它不仅告诉 Tokenizer 哪个 ID 对应哪个字符串，更重要的是它定义了**解码格式规则**：
    *   `lstrip` / `rstrip`: 解码时是否剥离左侧/右侧的空格？
    *   `single_word`: 是否必须作为一个独立的词出现？
    *   `special`: 这个 token ID 是否被认定为特殊 token？
*   **示例：**
    ```json
    "added_tokens_decoder": {
      "32000": {
        "content": "<tool_call>",
        "lstrip": false,
        "normalized": false,
        "rstrip": false,
        "single_word": false,
        "special": true
      },
      "32001": {
        "content": "<tool_response>",
        "lstrip": false,
        "normalized": false,
        "rstrip": false,
        "single_word": false,
        "special": true
      }
    }
    ```

</details>

<details open>
<summary><b>两者之间的关系与联动</b></summary>

在代码层面（HuggingFace Transformers），它们是**同一个动作在不同层面的投影**。

当你调用 `tokenizer.add_special_tokens({'additional_special_tokens': ['<tool_call>']})` 时：
1.  **高层意图** `additional_special_tokens` 被更新。
2.  **底层规则** Tokenizer 会在内部创建一个 `AddedToken` 对象（比如它会被分配一个新 ID $32000$），并将详细的解码规则（如 `lstrip=False`, `special=True`）写入 `added_tokens_decoder`。

因此，`tokenizer_config.json` 保存了 Tokenizer 的完整状态，以确保你重新加载时：
*   不仅知道有哪些特殊词（高层意图）；
*   还能完美还原每一个特殊词 ID 在解码时的行为（底层规则）。

</details>

### 总结

如果只是想简单添加一个 `<tool_call>` 并在 SFT 中正常训练，你**通常不需要手动编辑 `tokenizer_config.json`**，而是应该通过代码调用 `add_special_tokens` 方法（这会自动帮你更新和生成这两个字段）。如果你需要修改它，`additional_special_tokens` 是告诉 Tokenizer "不要切片这串字符"，而 `added_tokens_decoder` 是告诉底层引擎 "当模型输出这个 ID 时，具体按什么格式把它变回字符串"。