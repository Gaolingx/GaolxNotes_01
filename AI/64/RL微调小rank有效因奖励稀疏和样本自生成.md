# RL微调小rank有效因奖励稀疏和样本自生成

### 🧑‍💻 User

在我们给基模做rl后训练时候发现，只要模块选择正确（all linear），rank哪怕很小都能work是为什么，推测可能和rl奖励稀疏外加样本来自模型本身有关系

---

### 🤖 Assistant

你的观察非常敏锐，且你的推测（**奖励稀疏**以及**样本来自模型自身的On-policy分布**）直接命中了问题的核心！

在对基座模型进行强化学习（RL）后训练（如PPO、REINFORCE或DPO等偏好对齐算法）时，配置为 `all linear` 配合极小的 `rank`（例如 $r=1, 2, 4, 8$）确实能取得非常好的效果。这在当前的LLM对齐实践中是一个共识。

产生这种现象的核心原因可以归结为以下几个维度：

### 1. 你的推测验证：On-Policy 与 奖励稀疏性

你的直觉非常正确，RL 阶段的数据和奖励特性决定了小 `rank` 的有效性：

*   **样本来自模型本身（On-Policy 探索）：**
    在RL阶段，模型是在其**当前分布内**进行采样和探索的。RL的目标不是强行把模型扭转到一个完全陌生的输出分布上，而是**微调已有候选生成的概率**（把高Reward的概率调高，低Reward的调低）。因为动作空间（生成的Token）本来就是模型“懂”的，只需要在潜空间里做微小的向量偏移即可完成概率的重新分配，这不需要极大的参数容量。
*   **奖励稀疏与噪声（低秩作为天然正则化）：**
    RL的标量奖励信号往往是非常稀疏且充满噪声的（特别是基于RM打分的环境下）。如果 `rank` 过大，模型拥有了极高的自由度，它会极其容易对这些噪声过拟合，导致**奖励骇客（Reward Hacking）**和**灾难性遗忘（Catastrophic Forgetting）**，即模型不仅没学好对齐，反而把基模的通用能力破坏了。极小的 `rank` 在这里充当了非常强的**正则化器（Regularizer）**，限制了 $\Delta W$ 的更新幅度，保证了优化的平滑和稳定。

### 2. RL 任务的本质：是对齐（Alignment）而非注入知识（Knowledge Injection）

这是与SFT（监督微调）最大的区别：
*   在 SFT 阶段，如果我们需要模型学习新的垂直领域知识或复杂的格式，通常需要稍大一些的 `rank`（例如 $r=32, 64$），因为需要记忆新的信息。
*   但在 RL 阶段，模型已经具备了所需的全部知识和能力（经过了预训练和SFT）。RL的任务仅仅是**激发（Elicit）**和**规范（Align）**。改变说话的语气、拒绝有害指令、增加回复长度等行为模式的转变，其**内在维度（Intrinsic Dimension）是非常低的**。

### 3. 为什么必须是 `all linear`？

你提到“只要模块选择正确（`all linear`）”，这是非常关键的前提。

*   **广度胜于深度：** 只微调注意力机制中的 `q_proj` 和 `v_proj`，相当于在信息流转的局部制造了巨大的“阻力”。而选择 `all linear`（包括 Attention 的 Q, K, V, O 以及 MLP 的 Gate, Up, Down），意味着将原本需要集中在少数层的更新压力，**均摊**到了整个模型的各个计算节点上。
*   **全局协同：** 即使每个矩阵只有微小的秩（例如 $r=2$），但通过遍布模型所有层的协同微调，模型可以通过无数个极其微小的偏置（Nudges）叠加，实现复杂且全局的行为模式转换。数学上，多层低秩矩阵的非线性叠加，其表达能力远超单层的高秩矩阵。

<details>
<summary><b>点击展开：关于内在维度（Intrinsic Dimension）的数学直觉</b></summary>

Aghajanyan 等人在 2020 年的论文 *Intrinsic Dimensionality Explains the Effectiveness of Language Model Fine-Tuning* 中指出，预训练模型越大，其下游任务的内在维度越小。

在 LoRA 的公式中，$W' = W + B A$，其中 $B \in \mathbb{R}^{d \times r}$，$A \in \mathbb{R}^{r \times k}$。
在 RL 阶段，我们要优化的目标函数 $\mathcal{J}(\theta)$ 所在的流形（Manifold）非常平缓且维度极低。当目标仅仅是让某个轨迹的对数概率 $\log \pi_{\theta}(y|x)$ 产生微小偏移时，所需的特征子空间极小。

实验表明，对于对齐任务，特征值分解后 $\Delta W$ 中真正起作用的特征向量通常只有前一两个，其余全是噪声。因此，设置 $r > 8$ 往往只是在引入额外的噪声维度，并不会增加有效信息量。

</details>

### 总结建议

在 RLHF/RLAIF 阶段：
1.  **坚持 `all linear`**：这是保证微调平滑性的关键。
2.  **保持小 `rank`**：$r=4$ 或 $r=8$ 足够了，通常 $\alpha$ 设置为 $r$ 的1倍或2倍（如 `lora_alpha=16`）。
3.  **如果出现崩坏**：在小 `rank` 且 `all linear` 的前提下，如果 RL 训练很快训崩或 KL 散度爆炸，通常不再是 LoRA 结构的问题，而应该去排查 **Reward 模型的分数分布**、**PPO/DPO的超参（特别是 KL penalty 系数或学习率）**。

---

### 🧑‍💻 User

这和策略梯度有关系吗？为什么sft这种off policy就要高rank呢

---

### 🤖 Assistant

这是一个非常深刻的问题。你的直觉再次切中要害：**这不仅和数据分布（On-policy vs Off-policy）有关，更和底层的优化目标（策略梯度 Policy Gradient vs 交叉熵 Cross Entropy）有着本质的联系。**

我们可以从“策略梯度的数学特性”以及“SFT 的知识注入本质”两个方面来拆解为什么会产生这种 `rank` 需求的差异。

### 1. 为什么策略梯度（Policy Gradient）天生契合极小 Rank？

策略梯度（如 PPO）的核心逻辑是：根据奖励信号，**微调**已有动作的生成概率。

*   **梯度的方差极大（高 Rank 会变成毒药）：**
    策略梯度的核心公式是 $\nabla_\theta J(\theta) = \mathbb{E}_{\pi} [ \hat{A}_t \nabla_\theta \log \pi_\theta(a_t|s_t) ]$。
    这里的优势函数 $\hat{A}_t$ 是靠**采样**得来的，包含极大的方差和噪声。如果你的 `rank` 很高，模型的参数空间非常自由，它就会把这些方差当作真实信号去拟合（即“过拟合到单次采样的噪声上”），导致策略瞬间崩塌。极小的 `rank` 相当于一个**极其严格的低通滤波器（Low-pass Filter）**，强制模型只更新那些在多条样本中都稳定存在的“主成分”梯度方向。
*   **Log-Prob 的微扰（Nudging）无需高维空间：**
    在策略梯度中，我们不是要模型学会说一个它完全不会的词，而是针对它自己生成的句子（On-policy），告诉它“这句话不错，把这几个词的概率调高一点”。在神经网络的高维潜空间里，**顺着已有特征方向推一把**（Nudging），只需要一个秩为 1 的向量调整就足够了。

### 2. 为什么 SFT（Off-Policy / 行为克隆）需要高 Rank？

正如你所说，SFT 本质上是一种 Off-Policy 的学习（在 RL 中通常称为行为克隆 Behavioral Cloning）。它的数据来自外部专家（人类或 GPT-4），而不是模型自己生成的。这就带来了截然不同的要求：

*   **强制分布偏移（Forcing Distribution Shift）：**
    SFT 的目标是强行把模型的输出分布 $P_{model}$ 扭转到外部专家分布 $P_{data}$ 上。这其中包含着大量的“模型原本不想这么说，但必须这么说”的强制纠正。这需要跨越较大的潜空间距离，低秩矩阵的表达能力不足以完成这种大幅度的空间扭曲。
*   **信息注入与格式记忆：**
    SFT 阶段通常承担着让模型学习特定指令格式（如 `<|im_start|>`）、特定角色设定、甚至是垂直领域的新知识的任务。这些是实打实的**新信息（Information Entropy）**。香农定理告诉我们，信息的存储是需要容量的。高 `rank` 提供了足够的参数自由度来“记忆”这些新格式和新知识。
*   **交叉熵的“一刀切”特性：**
    SFT 使用的是交叉熵损失：$\mathcal{L}_{CE} = - \sum \log \pi_\theta(y^*_t | x, y^*_{<t})$。
    如果标准答案 $y^*$ 在模型当前字典里的概率极低，交叉熵会产生一个**极其巨大且明确**的惩罚梯度，强迫模型更新权重使得 $y^*$ 成为 top-1。这种“点对点”的精确拟合，对矩阵秩的要求远高于策略梯度中那种“模糊的方向性鼓励”。

---

<details>
<summary><b>点击展开：用“开车”打个比方，直观理解两者的差异</b></summary>

把微调大模型想象成**教人开车**：

1.  **SFT（高 Rank）：** 就像是在**驾校学车**。教练（专家数据）告诉你必须怎么握方向盘、怎么踩离合。你大脑里原本没有这些肌肉记忆（Off-policy，新知识），你需要大量的脑细胞（高 Rank）去死记硬背这些全新的操作规范。
2.  **RL / 策略梯度（低 Rank）：** 就像是你已经拿了驾照，自己在**公路上练车**。你已经会开了（On-policy），但是有时刹车踩得太急（奖励低）。这时候不需要重新长脑细胞去学“什么是刹车”，只需要在原有的神经回路上做一点点微调（低 Rank），形成条件反射：“下次刹车轻一点”。

因此，SFT 需要重塑神经元（高 Rank），而 RL 只是在调节突触权重的敏感度（低 Rank）。
</details>

### 总结

*   **RL阶段（小 Rank）**：优化目标是 Policy Gradient，高方差、顺势微调、防止噪声过拟合，因此需要极小的秩作为正则化。
*   **SFT阶段（高 Rank）**：优化目标是 Cross Entropy，任务是注入新知识、拟合外部真实分布（Off-policy），因此需要足够的参数容量来“记忆”。